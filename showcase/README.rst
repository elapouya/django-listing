To run this showcase demo:

* Ensure you are in the ``showcase/`` directory

If you have Poetry::

    poetry install
    poetry shell

Otherwise::

    virtualenv venv
    . venv/bin/activate
    pip install -r requirements.txt

Then::

    python ./manage.py runserver

Now go to to `<http://localhost:8000/>`_ and enjoy


